Ваш ответ:
1.1 Декларация `<!doctype html>` объявляет тип документа и его версию. В данном случае она указывает, что документ соответствует стандарту HTML5.
1.2 Она нужна браузеру для корректного отображения и интерпретации страницы в режиме соответствия стандартам (standards mode), что гарантирует единообразие в работе сайта в разных браузерах.
1.3 Она должна располагаться в самом начале HTML-документа, перед любыми другими тегами, включая `<html>`.

Ваш ответ:
2.1 Тег `<html>` является корневым (главным) элементом HTML-документа, который заключает в себе всё его содержимое.
2.2 Атрибут `lang="ru"` указывает браузеру, поисковым системам и вспомогательным технологиям (например, скринридерам), что основной язык текста на странице — русский. Это важно для правильного произношения текста, автоперевода и SEO.
2.3 Внутри `<html>` должны располагаться два основных раздела: `<head>` (для метаданных) и `<body>` (для основного содержимого, видимого пользователю).

Ваш ответ:
3.1 `<head>` — это служебный раздел (контейнер) документа, предназначенный для метаданных.
3.2 В нём хранится информация, не отображаемая напрямую на странице, но необходимая для её работы: заголовок (`<title>`), кодировка (`<meta charset>`), инструкции для отображения на мобильных устройствах (`<meta name="viewport">`), ссылки на стили (`<link>`) и скрипты.
3.3 `<head>` располагается внутри тега `<html>`, непосредственно перед открывающим тегом `<body>`.

Ваш ответ:
4.1 Тег `<meta>` предоставляет метаинформацию о документе (данные о данных), которую используют браузеры и поисковые системы.
4.2 `<meta charset="utf-8">` сообщает браузеру, что для кодирования символов на странице используется кодировка UTF-8. Это обеспечивает корректное отображение кириллицы и других специальных символов.
4.3 По шаблону он расположен внутри тега `<head>`.

Ваш ответ:
5.1 Мета-тег viewport управляет макетом и масштабированием страницы на мобильных устройствах.
5.2 Параметр `width=device-width` указывает браузеру, что ширина видимой области (viewport) должна быть равна ширине экрана устройства в пикселях независимо от его плотности.
5.3 Параметр `initial-scale=1` устанавливает начальный уровень масштабирования страницы в 100% (без увеличения).

Ваш ответ:
6.1 Тег `<title>` определяет заголовок (название) HTML-документа.
6.2 Текст из `<title>` отображается на вкладке браузера, в результатах поисковых систем, а также при добавлении страницы в закладки.
6.3 В коде `<title>` находится внутри тега `<head>`.

Ваш ответ:
7.1 Тег `<link>` определяет связь между текущим документом и внешним ресурсом.
7.2 Атрибут `rel="stylesheet"` указывает, что связываемый внешний ресурс является таблицей стилей (CSS-файлом).
7.3 `<link>` должен располагаться внутри тега `<head>`. Это связано с тем, что стили нужно загрузить и обработать до начала отрисовки содержимого `<body>` (для предотвращения мерцания незастилизованного контента — FOUC).

Ваш ответ:
8.1 Тег `<body>` является контейнером для всего содержимого веб-страницы, которое отображается в окне браузера.
8.2 Внутри `<body>` находится всё, что видит пользователь: тексты, изображения, ссылки, заголовки, видео и другие элементы.
8.3 `<body>` находится внутри `<html>`, сразу после закрывающего тега `</head>`.

Ваш ответ:
9.1 Семантический тег `<header>` обозначает вводную часть или шапку сайта, статьи или раздела.
9.2 На странице блога в `<header>` обычно помещают логотип, название блога, основную навигацию и, возможно, краткое описание.
9.3 В шаблоне `<header>` расположен внутри `<body>`. Внутри него находятся `<h1>` с названием блога и `<nav>` с навигацией.

Ваш ответ:
10.1 `<h1>` — это тег заголовка первого (наивысшего) уровня, который обозначает главный заголовок страницы или раздела.
10.2 `<h1>` в шапке обозначает главное название всего блога. `<h1>` внутри записи обозначает заголовок конкретной статьи/поста. Наличие двух `<h1>` допустимо в HTML5 при четкой структурной разбивке (например, внутри `<article>`), но для SEO часто рекомендуется один `<h1>` на страницу.
10.3 В шаблоне: первый `<h1>` находится внутри `<header>` и имеет текст "Мой дневник". Второй `<h1>` находится внутри первого `<article>` внутри `<main>` и имеет текст "Заголовок дневниковой записи".

Ваш ответ:
11.1 Тег `<nav>` обозначает раздел документа, содержащий навигационные ссылки.
11.2 Внутри него обычно размещают основную навигацию по сайту (ссылки на главные разделы) или по текущему документу (оглавление).
11.3 В шаблоне `<nav>` расположен внутри `<header>`. Внутри него находится список ссылок (`<a href="index.html">Главная</a>`).

Ваш ответ:
12.1 Тег `<a>` (anchor — якорь) используется для создания гиперссылок.
12.2 Атрибут `href` (hypertext reference) указывает URL-адрес, на который ведёт ссылка.
12.3 В шаблоне этот `<a>` расположен внутри тега `<nav>`, который, в свою очередь, находится внутри `<header>`.

Ваш ответ:
13.1 Тег `<main>` обозначает основное, уникальное содержание документа. Он не должен включать повторяющиеся на нескольких страницах блоки (шапку, подвал, сайдбар).
13.2 Его выделяют для улучшения семантики, доступности (скринридеры могут сразу перейти к основному контенту) и структуризации кода.
13.3 В шаблоне `<main>` расположен внутри `<body>`, после `<header>` и перед `<footer>`.

Ваш ответ:
14.1 `<article>` — это семантический тег, обозначающий независимый, самодостаточный фрагмент контента (статью, пост, новость, карточку товара), который может быть повторно использован.
14.2 В дневниковой записи `<article>` логично использовать для каждой отдельной записи, так как каждая из них представляет собой законченную мысль или рассказ.
14.3 В шаблоне: первый `<article>` находится сразу внутри `<main>` и содержит заголовок записи и вводный текст. Два других `<article>` находятся внутри `<section>` и представляют собой "прошлые записи" и "еще одна запись".

Ваш ответ:
15.1 `<section>` — это семантический тег, обозначающий тематическую группу контента, обычно с собственным заголовком.
15.2 В данном шаблоне секция группирует блок "Предыдущие записи", объединяя несколько записей (`<article>`) под общим заголовком `<h2>`.
15.3 В шаблоне `<section>` расположен внутри `<main>`, после первого `<article>` и перед `<aside>`.

Ваш ответ:
16.1 Тег `<p>` (paragraph) обозначает абзац текста.
16.2 Он используется для оформления обычного текстового содержимого, которое представляет собой логически завершенный отрывок.
16.3 В шаблоне `<p>` находятся: внутри первого `<article>` в `<main>` (текст записи), внутри `<article>` в `<section>` (текст прошлых записей), внутри `<aside>` (текст в боковой панели) и внутри `<footer>` (текст подвала).

Ваш ответ:
17.1 Тег `<aside>` обозначает раздел с содержимым, косвенно связанным с основным контентом (боковую панель, сноску, рекламный блок, архив).
17.2 На странице в него обычно помещают дополнительную информацию: цитаты, ссылки на архив, метки, рекламу, информацию об авторе.
17.3 В шаблоне `<aside>` расположен внутри `<main>`, после `<section>`. Внутри него находится заголовок `<h2>` и абзацы `<p>`.

Ваш ответ:
18.1 `<footer>` — это семантический тег, обозначающий нижний колонтитул (подвал) сайта, статьи или раздела.
18.2 В подвале сайта обычно размещают информацию об авторских правах, контактные данные, второстепенную навигацию, ссылки на социальные сети, логотип.
18.3 В шаблоне `<footer>` расположен внутри `<body>`, сразу после закрывающего тега `</main>`.

Ваш ответ:
19. Минимальный каркас HTML5-документа:
```html
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Document</title>
</head>
<body>

</body>
</html>
```

Ваш ответ:
20. Мета-теги должны находиться внутри тега `<head>`. Их нельзя ставить внизу `<body>`, потому что они содержат критически важные для корректной обработки страницы инструкции (кодировку, viewport, заголовок), которые браузер должен прочитать до начала рендеринга и стилизации содержимого `<body>`. Например, если метатег с кодировкой будет прочитан позже, текст может отобразиться "кракозябрами".

Ваш ответ:
21. Определить вложенность можно по отступам (которые задаются для удобства чтения) и, что главное, по порядку открывающих и закрывающих тегов. Закрывающий тег всегда завершает самый последний открытый блок.
Пример из шаблона:
```html
<main>
  <article>
    <h1>Заголовок дневниковой записи</h1>
    <p>Текст дневниковой записи...</p>
  </article>
</main>
```
Здесь `<article>` вложен в `<main>`, а `<h1>` и `<p>` — в `<article>`. Это видно по тому, что открывающий `<article>` идёт после открывающего `<main>`, а закрывающий `</article>` — перед закрывающим `</main>`.

Ваш ответ:
22.1 Блок `<nav>` расположен внутри тега `<header>`.
22.2 Внутри `<nav>` находится тег `<a href="index.html">Главная</a>`, который отвечает за создание гиперссылки на главную страницу.

Ваш ответ:
23.1 Внутри `<main>` находятся 3 крупных блока: `<article>`, `<section>` и `<aside>`.
23.2 Абзацы `<p>` находятся внутри: первого `<article>` в `<main>`, внутри каждого `<article>` в `<section>` (всего 2 раза), внутри `<aside>` и внутри `<footer>` (но `<footer>` уже вне `<main>`).

Ваш ответ:
24.1 `<section>` расположен внутри `<main>`, после первого `<article>` и перед `<aside>`.
24.2 Внутри `<section>` встречается 2 раза тег `<article>`. Внутри первого `<article>` внутри секции: `<h2>Прошлые записи</h2>` и `<p>Текст прошлой записи...</p>`. Внутри второго `<article>`: `<h2>Еще одна запись</h2>` и `<p>Текст еще одной записи...</p>`.

Ваш ответ:
25.1 Полный путь вложенности: `body → header → nav → a`.
25.2 Чтобы пользователь понял, куда ведёт ссылка, нужно изменить её текст с "Главная" на более информативный, например, "Мой дневник (Главная)" или просто оставить "Главная", что уже является стандартным и понятным обозначением. Для лучшей доступности можно добавить атрибут `title`: `<a href="index.html" title="Перейти на главную страницу дневника">Главная</a>`.